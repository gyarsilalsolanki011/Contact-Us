<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactUS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ContactUS" parent="Base.Theme.ContactUS" />

    <!-- My Designed Them -->
    <style name="Theme.Text.Bold" parent="Base.Theme.ContactUS">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.Text" parent="Base.Theme.ContactUS">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#0277BD</item>
        <item name="android:textColorHighlight">@color/black</item>
    </style>

    <style name="Theme.Button.Bold" parent="Base.Theme.ContactUS">
        <item name="android:textSize">28sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>